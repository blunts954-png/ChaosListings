[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci --only=production --ignore-scripts']

[phases.build]
cmds = ['npm install --only=dev', 'npx prisma generate', 'npm run build']

[start]
cmd = 'node dist/src/main.js'
